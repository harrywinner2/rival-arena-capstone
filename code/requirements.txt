# --- core LLM routing ---
litellm>=1.51.0          # unified router: OpenRouter + OpenAI direct
tenacity>=8.2.0          # retry/backoff on transient API errors

# --- data / numerics / stats ---
numpy>=1.26
scipy>=1.11
pandas>=2.1
statsmodels>=0.14        # factorial ANOVA, partial-eta2
pyyaml>=6.0
python-dotenv>=1.0

# --- viz ---
plotly>=5.18
matplotlib>=3.8
dash>=2.16               # live/replay dashboard
kaleido>=0.2.1           # static plotly image export for the deck

# --- dev ---
pytest>=7.4
