"""Rival-AI-Arena: a factorial arena for measuring when rival LLM agents
cooperate, defect, or collude.

Workstreams (mirroring the project plan):
    rival_arena.env      — standardized games + classical baselines
    rival_arena.harness  — two-model loop, channel ladder, monitor, logging
    rival_arena.metrics  — C/W/K/tau/sigma, message-structure, covert, stats
    rival_arena.viz      — live dashboard + publishable figures
"""

__version__ = "0.1.0"
